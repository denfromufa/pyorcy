- Compile only when the function is requested (so that the pure python
  development cycle does not get an annoying compilation step).

hesitations
- find a cleaner import mechanism (in particular without the "if 'pyximport'")
- Try to put the .pyx file together with the built files in ~/.pyxbld?
